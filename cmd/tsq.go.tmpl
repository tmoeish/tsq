{{- $dot := . }}
{{- $type := .TypeInfo.TypeName }}
{{- $varTbl := printf "Table%s" $type }}
{{- $varTblCols := printf "Table%sCols" $type }}
{{- $ptype := printf "*%s" $type }}
{{- $list := printf "[]*%s" $type }}
{{- $pageResp := printf "tsq.PageResp[%s]" $type }}

{{- $recv := printf "func (%s %s) " .Recv $type }}
{{- $precv := printf "func (%s *%s) " .Recv $type }}

{{- $getBy := printf "get%sBy" $type }}
{{- $GetBy := printf "Get%sBy" $type }}
{{- $ExistsBy := printf "Exists%sBy" $type }}

{{- $getActiveBy := printf "getActive%sBy" $type }}
{{- $GetActiveBy := printf "GetActive%sBy" $type }}
{{- $ExistsActiveBy := printf "ExistsActive%sBy" $type }}

{{- $CountBy := printf "Count%sBy" $type }}
{{- $ListBy := printf "List%sBy" $type }}
{{- $PageBy := printf "Page%sBy" $type }}

{{- $CountActiveBy := printf "CountActive%sBy" $type }}
{{- $listActiveBy := printf "listActive%sBy" $type }}
{{- $ListActiveBy := printf "ListActive%sBy" $type }}
{{- $PageActiveBy := printf "PageActive%sBy" $type }}

// Code generated by tsq-{{.TSQVersion}}. DO NOT EDIT.

package {{.TypeInfo.Package.Name}}

import (
	"context"
	tsqsql "database/sql"
{{- range $p, $a := .ImportMap }}
{{- if and (ne $p "database/sql") (ne $p "time") }}
	{{$a}} "{{$p}}"
{{- end }}
{{- end }}
{{- if NeedsGeneratedTimeImport . }}
	tsqtime "time"
{{- end }}
	"github.com/juju/errors"
	"github.com/tmoeish/tsq"
)

// =============================================================================
// Table Interface Implementation
// =============================================================================

// {{$varTbl}} implements the tsq.Table interface for {{$type}}.
var {{$varTbl}} tsq.Table = {{$type}}{}

// Column definitions for {{$type}} table.
var (
{{- range $f := .Fields }}
	{{$type}}_{{$f.Name}} = tsq.NewCol[{{$f | FieldType}}]({{$varTbl}}, "{{$f.Column}}", "{{$f.JsonTag}}", func(t any) any { return &t.({{$ptype}}).{{$f.Name}} })
{{- end }}
)

// {{$varTblCols}} is the list of columns for {{$type}} table.
var {{$varTblCols}} = []tsq.Column{
{{- range $f := .Fields }}
	{{$type}}_{{$f.Name}},
{{- end }}
}

// Table returns the database table name for {{$type}}.
func ({{$dot.Recv}} {{$type}}) Table() string { return "{{$dot.Table}}" }

// KwList returns columns that support keyword search for {{$type}}.
func ({{$dot.Recv}} {{$type}}) KwList() []tsq.Column {
	return []tsq.Column{
		{{- range $f := .KwList }}
		{{$type}}_{{$f}},
		{{- end }}
	}
}

func init() {
	tsq.RegisterTable(
		Table{{$type}},
		func(db *tsq.DbMap) {
			db.AddTableWithName(Table{{$type}}, "{{ $dot.Table }}").SetKeys({{ if $dot.AI }}true{{ else }}false{{ end }}, "{{ $dot.ID }}"){{ if $dot.VersionField }}.SetVersionCol("{{ $dot.VersionField }}"){{ end }}
		},
		func(db *tsq.DbMap) error {
{{- if $dot.UxList }}
			// Upsert unique indexes.
	{{- range $name, $idx := $dot.UxList }}
			if err := tsq.UpsertIndex(db, "{{ $dot.Table }}", true, "{{$idx.Name}}", []string{{"{"}}{{if $dot.DeletedAtField}}{{ FieldToCol $dot $dot.DeletedAtField }}, {{end}}{{ FieldsToCols $dot $idx.Fields }}{{"}"}}); err != nil {
				return errors.Annotate(err, "upsert {{$idx.Name}}@{{ $dot.Table }}")
			}
	{{- end }}
{{- end }}
{{- if $dot.IdxList }}

			// Upsert non-unique indexes.
	{{- range $name, $idx := $dot.IdxList }}
			if err := tsq.UpsertIndex(db, "{{ $dot.Table }}", false, "{{$idx.Name}}", []string{{"{"}}{{if $dot.DeletedAtField}}{{ FieldToCol $dot $dot.DeletedAtField }}, {{end}}{{ FieldsToCols $dot $idx.Fields }}{{"}"}}); err != nil {
				return errors.Annotate(err, "upsert {{$idx.Name}}@{{ $dot.Table }}")
			}
	{{- end }}
{{- end }}

			return nil
		},
	)
}


{{- if .DeletedAtField }}
// Active returns true if the {{$type}} record is not soft-deleted.
{{$precv}}Active() bool {
	return {{ SoftDeleteActiveExpr $dot.Recv $dot.DeletedAtField (index $dot.FieldMap $dot.DeletedAtField) }}
}
{{- end }}

// =============================================================================
// Query by Primary Key
// =============================================================================

{{- with $query := printf "%s%sQuery" $getBy $dot.ID }}
var {{$query}} *tsq.Query

func init() {
	var err error
	{{$query}}, err = tsq.
		Select({{$varTblCols}}...).
		Where({{$type}}_{{$dot.ID}}.EQVar()).
		Build()
	if err != nil {
		panic(errors.Annotate(err, "initialize {{$query}}"))
	}
}

	{{- with $name := printf "%s%s" $GetBy $dot.ID }}
// {{$name}} retrieves a {{$type}} record by its {{$dot.ID}}.
// Returns (nil, nil) if the record is not found.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
		{{ FieldVarName $dot.ID }} {{index $dot.FieldMap $dot.ID | FieldType}},
	) ({{$ptype}}, error) {
		row := &{{$type}}{}
		err := {{$query}}.Load(ctx, db, row, {{ FieldVarName $dot.ID }})
	if err != nil {
		if errors.Is(err, {{ GeneratedSQLRef "ErrNoRows" }}) {
			return nil, nil
		}

		return nil, errors.Trace(err)
	}

	return row, nil
}
	{{- end }}

	{{- with $name := printf "%s%sOrErr" $GetBy $dot.ID }}
// {{$name}} retrieves a {{$type}} record by its {{$dot.ID}}.
// Returns (nil, database/sql.ErrNoRows) if the record is not found.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
		{{ FieldVarName $dot.ID }} {{index $dot.FieldMap $dot.ID | FieldType}},
	) ({{$ptype}}, error) {
		row := &{{$type}}{}
		err := {{$query}}.Load(ctx, db, row, {{ FieldVarName $dot.ID }})
	if err != nil {
		if errors.Is(err, {{ GeneratedSQLRef "ErrNoRows" }}) {
			return nil, err
		}

		return nil, errors.Trace(err)
	}

	return row, nil
}
	{{- end }}
{{- end }}

{{- with $name := printf "%s%sIn" $ListBy $dot.ID }}
// {{$name}} retrieves multiple {{$type}} records by a set of {{$dot.ID}} values.
// Records not found are silently ignored.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
		{{ FieldSliceVarName $dot.ID }} ...{{index $dot.FieldMap $dot.ID | FieldType}},
	) ({{$list}}, error) {
		query, err := tsq.
			Select({{$varTblCols}}...).
			Where({{$type}}_{{$dot.ID}}.In({{ FieldSliceVarName $dot.ID }}...)).
		Build()
	if err != nil {
		return nil, errors.Annotate(err, "build query")
	}

	return tsq.List[{{$type}}](ctx, db, query)
}
{{- end }}

{{- with $name := printf "%s%sInOrErr" $ListBy $dot.ID }}
// {{$name}} retrieves multiple {{$type}} records by a set of {{$dot.ID}} values.
// Returns an error if any of the specified records are not found.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
		{{ FieldSliceVarName $dot.ID }} ...{{index $dot.FieldMap $dot.ID | FieldType}},
	) ({{$list}}, error) {
		query, err := tsq.
			Select({{$varTblCols}}...).
			Where({{$type}}_{{$dot.ID}}.In({{ FieldSliceVarName $dot.ID }}...)).
		Build()
	if err != nil {
		return nil, errors.Annotate(err, "build query")
	}

	list, err := tsq.List[{{$type}}](ctx, db, query)
	if err != nil {
		return nil, errors.Trace(err)
	}

	match := tsq.MatchByInputOrder({{ FieldSliceVarName $dot.ID }}, list, func(row *{{$type}}) {{index $dot.FieldMap $dot.ID | FieldType}} {
		return row.{{$dot.ID}}
	})
	if len(match.Missing) > 0 {
		return nil, errors.Errorf("{{$type}}(s) not found: %v", match.Missing)
	}
	return match.Ordered, nil
}
{{- end }}

{{- if .DeletedAtField }}
// =============================================================================
// Query Active Records by Primary Key
// =============================================================================

	{{- with $query := printf "%s%sQuery" $getActiveBy $dot.ID }}
var {{$query}} *tsq.Query

func init() {
	var err error
	{{$query}}, err = tsq.
		Select({{$varTblCols}}...).
		Where(
			{{ SoftDeleteActiveCond $type $dot.DeletedAtField (index $dot.FieldMap $dot.DeletedAtField) }},
			{{$type}}_{{$dot.ID}}.EQVar(),
		).
		Build()
	if err != nil {
		panic(errors.Annotate(err, "initialize {{$query}}"))
	}
}

		{{- with $name := printf "%s%s" $GetActiveBy $dot.ID }}
// {{$name}} retrieves an active (non-deleted) {{$type}} record by its {{$dot.ID}}.
// Returns (nil, nil) if the record is not found or has been soft-deleted.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
		{{ FieldVarName $dot.ID }} {{index $dot.FieldMap $dot.ID | FieldType}},
	) ({{$ptype}}, error) {
		row := &{{$type}}{}
		err := {{$query}}.Load(ctx, db, row, {{ FieldVarName $dot.ID }})
	if err != nil {
		if errors.Is(err, {{ GeneratedSQLRef "ErrNoRows" }}) {
			return nil, nil
		}

		return nil, errors.Trace(err)
	}

	return row, nil
}
		{{- end }}

		{{- with $name := printf "%s%sOrErr" $GetActiveBy $dot.ID }}
// {{$name}} retrieves an active (non-deleted) {{$type}} record by its {{$dot.ID}}.
// Returns (nil, database/sql.ErrNoRows) if the record is not found or has been soft-deleted.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
		{{ FieldVarName $dot.ID }} {{index $dot.FieldMap $dot.ID | FieldType}},
	) ({{$ptype}}, error) {
		row := &{{$type}}{}
		err := {{$query}}.Load(ctx, db, row, {{ FieldVarName $dot.ID }})
	if err != nil {
		if errors.Is(err, {{ GeneratedSQLRef "ErrNoRows" }}) {
			return nil, err
		}

		return nil, errors.Trace(err)
	}

	return row, nil
}
		{{- end }}
	{{- end }}

	{{- with $name := printf "%s%sIn" $ListActiveBy $dot.ID }}
// {{$name}} retrieves multiple active (non-deleted) {{$type}} records by a set of {{$dot.ID}} values.
// Records not found or soft-deleted are silently ignored.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
		{{ FieldSliceVarName $dot.ID }} ...{{index $dot.FieldMap $dot.ID | FieldType}},
) ({{$list}}, error) {
	query, err := tsq.
		Select({{$varTblCols}}...).
		Where(
			{{ SoftDeleteActiveCond $type $dot.DeletedAtField (index $dot.FieldMap $dot.DeletedAtField) }},
			{{$type}}_{{$dot.ID}}.In({{ FieldSliceVarName $dot.ID }}...),
		).
		Build()
	if err != nil {
		return nil, errors.Annotate(err, "build query")
	}
	list, err := tsq.List[{{$type}}](ctx, db, query)
	if err != nil {
		return nil, errors.Trace(err)
	}
	return list, nil
}
	{{- end }}

	{{- with $name := printf "%s%sInOrErr" $ListActiveBy $dot.ID }}
// {{$name}} retrieves multiple active (non-deleted) {{$type}} records by a set of {{$dot.ID}} values.
// Returns an error if any of the specified active records are not found.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
		{{ FieldSliceVarName $dot.ID }} ...{{index $dot.FieldMap $dot.ID | FieldType}},
	) ({{$list}}, error) {
	query, err := tsq.
		Select({{$varTblCols}}...).
		Where(
			{{ SoftDeleteActiveCond $type $dot.DeletedAtField (index $dot.FieldMap $dot.DeletedAtField) }},
			{{$type}}_{{$dot.ID}}.In({{ FieldSliceVarName $dot.ID }}...),
		).
		Build()
	if err != nil {
		return nil, errors.Annotate(err, "build query")
	}

	var list {{$list}}
	list, err = tsq.List[{{$type}}](ctx, db, query)
	if err != nil {
		return nil, errors.Trace(err)
	}
	match := tsq.MatchByInputOrder({{ FieldSliceVarName $dot.ID }}, list, func(row *{{$type}}) {{index $dot.FieldMap $dot.ID | FieldType}} {
		return row.{{$dot.ID}}
	})
	if len(match.Missing) > 0 {
		return nil, errors.Errorf("{{$type}}(s) not found: %v", match.Missing)
	}
	return match.Ordered, nil
}
	{{- end }}
{{- end }}

// =============================================================================
// CRUD Operations
// =============================================================================

// Insert inserts a new {{$type}} record into the database.
// Automatically sets creation and modification timestamps if configured.
{{$precv}}Insert(
	ctx context.Context,
	db tsq.SqlExecutor,
) error {
{{- if $dot.CreatedAtField }}
	{{$dot.Recv}}.{{$dot.CreatedAtField}} = {{ TimestampNowValue (index $dot.FieldMap $dot.CreatedAtField) }}
{{- end }}
{{- if $dot.UpdatedAtField }}
	{{$dot.Recv}}.{{$dot.UpdatedAtField}} = {{ TimestampNowValue (index $dot.FieldMap $dot.UpdatedAtField) }}
{{- end }}
	err := tsq.Insert(ctx, db, {{$dot.Recv}})
	if err != nil {
		return errors.Annotate(err, tsq.PrettyJSON({{$dot.Recv}}))
	}

	return nil
}

// Update updates an existing {{$type}} record in the database.
// Automatically updates the modification timestamp if configured.
{{$precv}}Update(
	ctx context.Context,
	db tsq.SqlExecutor,
) error {
{{- if $dot.UpdatedAtField }}
	{{$dot.Recv}}.{{$dot.UpdatedAtField}} = {{ TimestampNowValue (index $dot.FieldMap $dot.UpdatedAtField) }}
{{- end }}
	err := tsq.Update(ctx, db, {{$dot.Recv}})
	if err != nil {
		return errors.Annotate(err, tsq.PrettyJSON({{$dot.Recv}}))
	}

	return nil
}

// Delete permanently removes a {{$type}} record from the database.
{{$precv}}Delete(
	ctx context.Context,
	db tsq.SqlExecutor,
) error {
	err := tsq.Delete(ctx, db, {{$dot.Recv}})
	if err != nil {
		return errors.Annotate(err, tsq.PrettyJSON({{$dot.Recv}}))
	}

	return nil
}

{{- if .DeletedAtField }}
// SoftDelete marks a {{$type}} record as deleted without removing it from the database.
// If dt is set, uses the provided tombstone value; otherwise uses the current time.
{{$precv}}SoftDelete(
	ctx context.Context,
	db tsq.SqlExecutor,
	dt {{ SoftDeleteParamType (index $dot.FieldMap $dot.DeletedAtField) }},
) error {
	if {{ SoftDeleteParamSetExpr "dt" (index $dot.FieldMap $dot.DeletedAtField) }} {
		{{$dot.Recv}}.{{$dot.DeletedAtField}} = dt
	} else {
		{{$dot.Recv}}.{{$dot.DeletedAtField}} = {{ SoftDeleteNowValue (index $dot.FieldMap $dot.DeletedAtField) }}
	}
{{- if $dot.UpdatedAtField }}
	{{$dot.Recv}}.{{$dot.UpdatedAtField}} = {{ TimestampNowValue (index $dot.FieldMap $dot.UpdatedAtField) }}
{{- end }}
	err := tsq.Update(ctx, db, {{$dot.Recv}})
	if err != nil {
		return errors.Annotate(err, tsq.PrettyJSON({{$dot.Recv}}))
	}

	return nil
}
{{- end }}

{{- with $name := printf "%sQuery" $ListBy }}
// {{$name}} executes a custom query to retrieve {{$type}} records.
func {{$name}}(
	ctx context.Context,
	tx tsq.SqlExecutor,
	qb *tsq.Query,
	args ...any,
) ({{$list}}, error) {
	return tsq.List[{{$type}}](ctx, tx, qb, args...)
}
{{- end }}

{{- with $name := printf "%sQuery" $PageBy }}
// {{$name}} executes a custom query with pagination to retrieve {{$type}} records.
func {{$name}}(
	ctx context.Context,
	tx tsq.SqlExecutor,
	page *tsq.PageReq,
	qb *tsq.Query,
	args ...any,
) (*{{$pageResp}}, error) {
	return tsq.Page[{{$type}}](ctx, tx, page, qb, args...)
}
{{- end }}

// =============================================================================
// List All Records
// =============================================================================

{{- with $query := printf "list%sQuery" $type }}
// {{$query}} is the base query for retrieving all {{$type}} records.
var {{$query}} *tsq.Query

func init() {
	var err error
	{{$query}}, err = tsq.
		Select({{$varTblCols}}...).
		KwSearch(Table{{$type}}.KwList()...).
		Build()
	if err != nil {
		panic(errors.Annotate(err, "initialize {{$query}}"))
	}
}

	{{- with $name := printf "Count%s" $type }}
// {{$name}} returns the total count of {{$type}} records.
func {{$name}}(
	ctx context.Context,
	tx tsq.SqlExecutor,
) (int64, error) {
	return {{$query}}.Count64(ctx, tx)
}
	{{- end }}

	{{- with $name := printf "List%s" $type }}
// {{$name}} retrieves all {{$type}} records from the database.
func {{$name}}(
	ctx context.Context,
	tx tsq.SqlExecutor,
) ({{$list}}, error) {
	return tsq.List[{{$type}}](ctx, tx, {{$query}})
}
	{{- end }}

	{{- with $name := printf "Page%s" $type }}
// {{$name}} retrieves {{$type}} records with pagination support.
func {{$name}}(
	ctx context.Context,
	tx tsq.SqlExecutor,
	page *tsq.PageReq,
) (*{{$pageResp}}, error) {
	return tsq.Page[{{$type}}](ctx, tx, page, {{$query}})
}
	{{- end }}
{{- end }}

{{- if .DeletedAtField }}
// =============================================================================
// List Active Records
// =============================================================================

	{{- with $query := printf "listActive%sQuery" $type }}
// {{$query}} is the base query for retrieving active (non-deleted) {{$type}} records.
var {{$query}} *tsq.Query

func init() {
	var err error
	{{$query}}, err = tsq.
		Select({{$varTblCols}}...).
		Where({{ SoftDeleteActiveCond $type $dot.DeletedAtField (index $dot.FieldMap $dot.DeletedAtField) }}).
		KwSearch(Table{{$type}}.KwList()...).
		Build()
	if err != nil {
		panic(errors.Annotate(err, "initialize {{$query}}"))
	}
}

		{{- with $name := printf "CountActive%s" $type }}
// {{$name}} returns the count of active (non-deleted) {{$type}} records.
func {{$name}}(
	ctx context.Context,
	tx tsq.SqlExecutor,
) (int64, error) {
	return {{$query}}.Count64(ctx, tx)
}
		{{- end }}

		{{- with $name := printf "ListActive%s" $type }}
// {{$name}} retrieves all active (non-deleted) {{$type}} records.
func {{$name}}(
	ctx context.Context,
	tx tsq.SqlExecutor,
) ({{$list}}, error) {
	return tsq.List[{{$type}}](ctx, tx, {{$query}})
}
		{{- end }}

		{{- with $name := printf "PageActive%s" $type }}
// {{$name}} retrieves active (non-deleted) {{$type}} records with pagination support.
func {{$name}}(
	ctx context.Context,
	tx tsq.SqlExecutor,
	page *tsq.PageReq,
) (*{{$pageResp}}, error) {
	return tsq.Page[{{$type}}](ctx, tx, page, {{$query}})
}
		{{- end }}
	{{- end }}
{{- end }}

// =============================================================================
// Query by Unique Indexes
// =============================================================================

{{- range $ux := .UxList }}
	{{- with $query := printf "%s%sQuery" $getBy (JoinAnd $ux.Fields) }}
var {{$query}} *tsq.Query

func init() {
	var err error
	{{$query}}, err = tsq.
		Select({{$varTblCols}}...).
		Where(
			{{- range $f := $ux.Fields }}
			{{$type}}_{{$f}}.EQVar(),
			{{- end }}
		).
		KwSearch(Table{{$type}}.KwList()...).
		Build()
	if err != nil {
		panic(errors.Annotate(err, "initialize {{$query}}"))
	}
}

		{{- with $name := printf "%s%s" $GetBy (JoinAnd $ux.Fields) }}
// {{$name}} retrieves a {{$type}} record by unique index {{$ux.Name}}.
// Returns (nil, nil) if the record is not found.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
			{{- range $f := $ux.Fields }}
		{{ FieldVarName $f }} {{ index $dot.FieldMap $f | FieldType }},
			{{- end }}
) ({{$ptype}}, error) {
	query := {{$query}}

	row := &{{$type}}{}
	err := query.Load(ctx, db, row,
		{{- range $f := $ux.Fields }}
			{{ FieldVarName $f }},
		{{- end }}
	)
	if err != nil {
		if errors.Is(err, {{ GeneratedSQLRef "ErrNoRows" }}) {
			return nil, nil
		}

		return nil, errors.Trace(err)
	}

	return row, nil
}
		{{- end }}

		{{- with $name := printf "%s%sOrErr" $GetBy (JoinAnd $ux.Fields) }}
// {{$name}} retrieves a {{$type}} record by unique index {{$ux.Name}}.
// Returns (nil, database/sql.ErrNoRows) if the record is not found.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
			{{- range $f := $ux.Fields }}
		{{ FieldVarName $f }} {{ index $dot.FieldMap $f | FieldType }},
			{{- end }}
) ({{$ptype}}, error) {
	query := {{$query}}

	row := &{{$type}}{}
	err := query.Load(ctx, db, row,
		{{- range $f := $ux.Fields }}
			{{ FieldVarName $f }},
		{{- end }}
	)
	if err != nil {
		if errors.Is(err, {{ GeneratedSQLRef "ErrNoRows" }}) {
			return nil, err
		}

		return nil, errors.Trace(err)
	}

	return row, nil
}
		{{- end }}

		{{- with $name := printf "%s%s" $ExistsBy (JoinAnd $ux.Fields) }}
// {{$name}} checks whether a {{$type}} record exists by unique index {{$ux.Name}}.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
			{{- range $f := $ux.Fields }}
		{{ FieldVarName $f }} {{ index $dot.FieldMap $f | FieldType }},
			{{- end }}
) (bool, error) {
	query := {{$query}}

	rs, err := query.Exists(ctx, db,
		{{- range $f := $ux.Fields }}
			{{ FieldVarName $f }},
		{{- end }}
	)
	return rs, errors.Trace(err)
}
		{{- end }}
	{{- end }}
{{- end }}

{{- if .DeletedAtField }}
// =============================================================================
// Query Active Records by Unique Indexes
// =============================================================================

	{{- range $ux := .UxList }}
		{{- with $query := printf "%s%sQuery" $getActiveBy (JoinAnd $ux.Fields) }}
// {{$query}} queries active (non-deleted) {{$type}} records by unique index {{$ux.Name}}.
var {{$query}} *tsq.Query

func init() {
	var err error
	{{$query}}, err = tsq.
		Select({{$varTblCols}}...).
		Where(
			{{ SoftDeleteActiveCond $type $dot.DeletedAtField (index $dot.FieldMap $dot.DeletedAtField) }},
				{{- range $f := $ux.Fields }}
			{{$type}}_{{$f}}.EQVar(),
				{{- end }}
		).
		KwSearch(Table{{$type}}.KwList()...).
		Build()
	if err != nil {
		panic(errors.Annotate(err, "initialize {{$query}}"))
	}
}

			{{- with $name := printf "%s%s" $GetActiveBy (JoinAnd $ux.Fields) }}
// {{$name}} retrieves an active (non-deleted) {{$type}} record by unique index {{$ux.Name}}.
// Returns (nil, nil) if the record is not found or has been soft-deleted.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
				{{- range $f := $ux.Fields }}
		{{ FieldVarName $f }} {{ index $dot.FieldMap $f | FieldType }},
				{{- end }}
) ({{$ptype}}, error) {
	query := {{$query}}

	row := &{{$type}}{}
	err := query.Load(ctx, db, row,
		{{- range $f := $ux.Fields }}
			{{ FieldVarName $f }},
		{{- end }}
	)
	if err != nil {
		if errors.Is(err, {{ GeneratedSQLRef "ErrNoRows" }}) {
			return nil, nil
		}

		return nil, errors.Trace(err)
	}

	return row, nil
}
			{{- end }}

			{{- with $name := printf "%s%sOrErr" $GetActiveBy (JoinAnd $ux.Fields) }}
// {{$name}} retrieves an active (non-deleted) {{$type}} record by unique index {{$ux.Name}}.
// Returns (nil, database/sql.ErrNoRows) if the record is not found or has been soft-deleted.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
				{{- range $f := $ux.Fields }}
		{{ FieldVarName $f }} {{ index $dot.FieldMap $f | FieldType }},
				{{- end }}
) ({{$ptype}}, error) {
	query := {{$query}}

		row := &{{$type}}{}
	err := query.Load(ctx, db, row,
		{{- range $f := $ux.Fields }}
			{{ FieldVarName $f }},
		{{- end }}
	)
	if err != nil {
		if errors.Is(err, {{ GeneratedSQLRef "ErrNoRows" }}) {
			return nil, err
		}

		return nil, errors.Trace(err)
	}

	return row, nil
}
			{{- end }}

			{{- with $name := printf "%s%s" $ExistsActiveBy (JoinAnd $ux.Fields) }}
// {{$name}} checks whether an active (non-deleted) {{$type}} record exists by unique index {{$ux.Name}}.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
				{{- range $f := $ux.Fields }}
		{{ FieldVarName $f }} {{ index $dot.FieldMap $f | FieldType }},
				{{- end }}
) (bool, error) {
	query := {{$query}}

		rs, err := query.Exists(ctx, db,
		{{- range $f := $ux.Fields }}
			{{ FieldVarName $f }},
		{{- end }}
	)
	return rs, errors.Trace(err)
}
			{{- end }}
		{{- end }}
	{{- end }}
{{- end }}

{{- if .QueryList }}
// =============================================================================
// Query by Indexes
// =============================================================================
{{- end }}

{{- range $idx := .QueryList }}
	{{- $indexLabel := or $idx.SourceName $idx.Name }}
	{{- if $idx.IsSet }}
		{{- with $query := printf "%s%sQuery" $ListBy $idx.Name }}
// {{$query}} queries {{$type}} records by index {{$indexLabel}} using a cached IN-clause query.
var {{$query}} *tsq.Query

func init() {
	var err error
	{{$query}}, err = tsq.
		Select({{$varTblCols}}...).
		Where(
		{{- range $i, $f := $idx.Fields }}
			{{- if eq $i (Sub1 (len $idx.Fields)) }}
			{{$type}}_{{$f}}.InVar(),
			{{- else }}
			{{$type}}_{{$f}}.EQVar(),
			{{- end }}
		{{- end }}
		).
		Build()
	if err != nil {
		panic(errors.Annotate(err, "initialize {{$query}}"))
	}
}
		{{- end }}

	// List{{$type}}By{{$idx.Name}} retrieves {{$type}} records by index {{$idx.Name}} using IN clause for batch querying.
func List{{$type}}By{{$idx.Name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
		{{- range $i, $f := $idx.Fields }}
			{{- if eq $i (Sub1 (len $idx.Fields)) }}
		{{ FieldSliceVarName $f }} ...{{ (index $.FieldMap $f) | FieldType }},
			{{- else }}
		{{ FieldVarName $f }} {{ (index $.FieldMap $f) | FieldType }},
			{{- end }}
		{{- end }}
) ({{$list}}, error) {
	query := List{{$type}}By{{$idx.Name}}Query

	list, err := tsq.List[{{$type}}](ctx, db, query,
		{{- range $i, $f := $idx.Fields }}
			{{- if eq $i (Sub1 (len $idx.Fields)) }}
			{{ FieldSliceVarName $f }},
			{{- else }}
			{{ FieldVarName $f }},
			{{- end }}
		{{- end }}
	)
	if err != nil {
		return nil, errors.Annotate(err, "query by index {{$indexLabel}}")
	}
	return list, nil
}
	{{- else }}
		{{- with $query := printf "%s%sQuery" $ListBy $idx.Name }}
// {{$query}} queries {{$type}} records by index {{$indexLabel}}.
var {{$query}} *tsq.Query

func init() {
	var err error
	{{$query}}, err = tsq.
		Select({{$varTblCols}}...).
		Where(
				{{- range $f := $idx.Fields }}
			{{$type}}_{{$f}}.EQVar(),
				{{- end }}
		).
		KwSearch(Table{{$type}}.KwList()...).
		Build()
	if err != nil {
		panic(errors.Annotate(err, "initialize {{$query}}"))
	}
}

			{{- with $name := printf "%s%s" $CountBy $idx.Name }}
// {{$name}} returns the count of {{$type}} records matching index {{$indexLabel}}.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
				{{- range $f := $idx.Fields }}
		{{ FieldVarName $f }} {{ index $dot.FieldMap $f | FieldType }},
				{{- end }}
) (int64, error) {
	query := {{$query}}

	rs, err := query.Count64(ctx, db,
		{{- range $f := $idx.Fields }}
			{{ FieldVarName $f }},
		{{- end }}
	)
	if err != nil {
		return 0, errors.Annotate(err, "query by index {{$indexLabel}}")
	}
	return rs, nil
}
			{{- end }}

			{{- with $name := printf "%s%s" $ListBy $idx.Name }}
// {{$name}} retrieves {{$type}} records by index {{$indexLabel}}.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
				{{- range $f := $idx.Fields }}
		{{ FieldVarName $f }} {{ index $dot.FieldMap $f | FieldType }},
				{{- end }}
) ({{$list}}, error) {
	query := {{$query}}

	data, err := tsq.List[{{$type}}](ctx, db, query,
		{{- range $f := $idx.Fields }}
			{{ FieldVarName $f }},
		{{- end }}
	)
	if err != nil {
		return nil, errors.Annotate(err, "query by index {{$indexLabel}}")
	}
	return data, nil
}
			{{- end }}

			{{- with $name := printf "%s%s" $PageBy $idx.Name }}
// {{$name}} retrieves {{$type}} records by index {{$indexLabel}} with pagination support.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
	page *tsq.PageReq,
				{{- range $f := $idx.Fields }}
		{{ FieldVarName $f }} {{ index $dot.FieldMap $f | FieldType }},
				{{- end }}
) (*{{$pageResp}}, error) {
	query := {{$query}}

	rs, err := tsq.Page[{{$type}}](ctx, db, page, query,
		{{- range $f := $idx.Fields }}
			{{ FieldVarName $f }},
		{{- end }}
	)
	if err != nil {
		return nil, errors.Annotate(err, "query by index {{$indexLabel}}")
	}
	return rs, nil
}
			{{- end }}
		{{- end }}
	{{- end }}
{{- end }}

{{- if .DeletedAtField }}
// =============================================================================
// Query Active Records by Indexes
// =============================================================================

	{{- range $idx := .QueryList }}
		{{- $indexLabel := or $idx.SourceName $idx.Name }}
		{{- if $idx.IsSet }}
			{{- with $query := printf "%s%sQuery" $ListActiveBy $idx.Name }}
// {{$query}} queries active (non-deleted) {{$type}} records by index {{$indexLabel}} using a cached IN-clause query.
var {{$query}} *tsq.Query

func init() {
	var err error
	{{$query}}, err = tsq.
		Select({{$varTblCols}}...).
		Where(
			{{ SoftDeleteActiveCond $type $dot.DeletedAtField (index $dot.FieldMap $dot.DeletedAtField) }},
				{{- range $i, $f := $idx.Fields }}
					{{- if eq $i (Sub1 (len $idx.Fields)) }}
			{{$type}}_{{$f}}.InVar(),
					{{- else }}
			{{$type}}_{{$f}}.EQVar(),
					{{- end }}
				{{- end }}
		).
		Build()
	if err != nil {
		panic(errors.Annotate(err, "initialize {{$query}}"))
	}
}
			{{- end }}

			{{- with $name := printf "%s%s" $ListActiveBy $idx.Name }}
// {{$name}} retrieves active (non-deleted) {{$type}} records by index {{$indexLabel}} using IN clause for batch querying.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
				{{- range $i, $f := $idx.Fields }}
					{{- if eq $i (Sub1 (len $idx.Fields)) }}
		{{ FieldSliceVarName $f }} ...{{ (index $.FieldMap $f) | FieldType }},
					{{- else }}
		{{ FieldVarName $f }} {{ (index $.FieldMap $f) | FieldType }},
					{{- end }}
				{{- end }}
) ({{$list}}, error) {
	query := {{ printf "%s%sQuery" $ListActiveBy $idx.Name }}
	list, err := tsq.List[{{$type}}](ctx, db, query,
		{{- range $i, $f := $idx.Fields }}
			{{- if eq $i (Sub1 (len $idx.Fields)) }}
			{{ FieldSliceVarName $f }},
			{{- else }}
			{{ FieldVarName $f }},
			{{- end }}
		{{- end }}
	)
	if err != nil {
		return nil, errors.Annotate(err, "query by index {{$indexLabel}}")
	}
	return list, nil
}
			{{- end }}
		{{- else }}
			{{- with $query := printf "%s%sQuery" $listActiveBy $idx.Name }}
// {{$query}} queries active (non-deleted) {{$type}} records by index {{$indexLabel}}.
var {{$query}} *tsq.Query

func init() {
	var err error
	{{$query}}, err = tsq.
		Select({{$varTblCols}}...).
		Where(
			{{ SoftDeleteActiveCond $type $dot.DeletedAtField (index $dot.FieldMap $dot.DeletedAtField) }},
					{{- range $f := $idx.Fields }}
			{{$type}}_{{$f}}.EQVar(),
					{{- end }}
		).
		KwSearch(Table{{$type}}.KwList()...).
		Build()
	if err != nil {
		panic(errors.Annotate(err, "initialize {{$query}}"))
	}
}

				{{- with $name := printf "%s%s" $CountActiveBy $idx.Name }}
// {{$name}} returns the count of active (non-deleted) {{$type}} records matching index {{$indexLabel}}.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
					{{- range $f := $idx.Fields }}
		{{ FieldVarName $f }} {{ index $dot.FieldMap $f | FieldType }},
					{{- end }}
) (int64, error) {
	query := {{$query}}

	rs, err := query.Count64(ctx, db,
		{{- range $f := $idx.Fields }}
			{{ FieldVarName $f }},
		{{- end }}
	)
	if err != nil {
		return 0, errors.Annotate(err, "query by index {{$indexLabel}}")
	}
	return rs, nil
}
				{{- end }}

				{{- with $name := printf "%s%s" $ListActiveBy $idx.Name }}
// {{$name}} retrieves active (non-deleted) {{$type}} records by index {{$indexLabel}}.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
					{{- range $f := $idx.Fields }}
		{{ FieldVarName $f }} {{ index $dot.FieldMap $f | FieldType }},
					{{- end }}
) ({{$list}}, error) {
	query := {{$query}}

	data, err := tsq.List[{{$type}}](ctx, db, query,
		{{- range $f := $idx.Fields }}
			{{ FieldVarName $f }},
		{{- end }}
	)
	if err != nil {
		return nil, errors.Annotate(err, "query by index {{$indexLabel}}")
	}
	return data, nil
}
				{{- end }}

				{{- with $name := printf "%s%s" $PageActiveBy $idx.Name }}
// {{$name}} retrieves active (non-deleted) {{$type}} records by index {{$indexLabel}} with pagination support.
func {{$name}}(
	ctx context.Context,
	db tsq.SqlExecutor,
	page *tsq.PageReq,
					{{- range $f := $idx.Fields }}
		{{ FieldVarName $f }} {{ index $dot.FieldMap $f | FieldType }},
					{{- end }}
) (*{{$pageResp}}, error) {
	query := {{$query}}

	rs, err := tsq.Page[{{$type}}](ctx, db, page, query,
		{{- range $f := $idx.Fields }}
			{{ FieldVarName $f }},
		{{- end }}
	)
	if err != nil {
		return nil, errors.Annotate(err, "query by index {{$indexLabel}}")
	}
	return rs, nil
}
				{{- end }}
			{{- end }}

		{{- end }}
	{{- end }}
{{- end }}
