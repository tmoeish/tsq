-- Code generated by tsq-v3.7.0. DO NOT EDIT.

-- Dialect: mysql

-- Table: category

CREATE TABLE IF NOT EXISTS `category` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `created_at` DATETIME,
    `description` VARCHAR(4096) NOT NULL,
    `name` VARCHAR(200) NOT NULL,
    `type` BIGINT NOT NULL
);

ALTER TABLE `category` ADD UNIQUE INDEX `ux_category_name`(`name`);


-- Table: item

CREATE TABLE IF NOT EXISTS `item` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `created_at` DATETIME,
    `category_id` BIGINT NOT NULL,
    `name` VARCHAR(200) NOT NULL,
    `price` BIGINT NOT NULL
);

ALTER TABLE `item` ADD INDEX `idx_category`(`category_id`);

ALTER TABLE `item` ADD UNIQUE INDEX `ux_item_name`(`name`);


-- Table: order

CREATE TABLE IF NOT EXISTS `order` (
    `uid` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at` DATETIME,
    `deleted_at` BIGINT NOT NULL DEFAULT 0,
    `version` BIGINT NOT NULL DEFAULT 1,
    `amount` BIGINT NOT NULL,
    `item_id` BIGINT NOT NULL,
    `price` BIGINT NOT NULL,
    `status` BIGINT NOT NULL,
    `user_id` BIGINT NOT NULL
);

ALTER TABLE `order` ADD INDEX `idx_item`(`item_id`);

ALTER TABLE `order` ADD INDEX `idx_order_user_id_item_id`(`user_id`, `item_id`);


-- Table: org

CREATE TABLE IF NOT EXISTS `org` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `created_at` DATETIME,
    `name` TEXT NOT NULL
);

ALTER TABLE `org` ADD UNIQUE INDEX `ux_org_name`(`name`);


-- Table: user

CREATE TABLE IF NOT EXISTS `user` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `created_at` DATETIME,
    `email` TEXT NOT NULL,
    `name` TEXT NOT NULL,
    `org_id` BIGINT NOT NULL
);

ALTER TABLE `user` ADD UNIQUE INDEX `ux_user_name`(`name`);
