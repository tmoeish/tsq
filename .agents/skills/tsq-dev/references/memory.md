# 项目内存

这个代码库无法自己告诉你的东西：事故及其根本原因、决定及其推理、不明显的运行时行为、值得不再重复的死胡同。不要复述规则（`AGENTS.md` 拥有）、包布局（`architecture.md` 拥有）或使用者契约（`skills/tsq` 拥有）——重复的陈述注定漂移。按主题归入已有小节，不要按日期另起一条。**就地纠正错误的条目、删除陈旧的条目，永远不要追加自相矛盾的内容**：过时的内存比没有内存更糟，人会相信它。

## 一条记录什么时候该走

这些文件的长度是**每次会话都要付的成本**；但"处理完就删"也是错的，有些记录的价值恰恰在问题消失之后才开始——它解释了代码为什么长这样。**判据只有一条：删掉它之后，有人会不会重犯、或者重新调查一遍？**

| 类型 | 事情了结之后 |
| --- | --- |
| **搁置项**（`已知未处理：` 开头） | **删掉**，问题没了记录就没有读者 |
| **事故 + 根因** | **留**，有门禁挡着了就压成一行指向那道门 |
| **决定 + 理由**、**死胡同** | **永久留**，删了下一个人会改回去或重试一遍 |

`make memory-check` **无条件**守着本文件加 `memory/` 的**总**行数上限——拆成几份是为了让一次会话只读其中一两份，不是为了把上限拆成六个各自够用的小额度。**写满了先合并同主题的条目**（新学到的并进它属于的那条），而不是删掉一条还成立的记录。

## 按主题读

| 你在动（**只读与这波有关的那一两份**） | 读 |
| --- | --- |
| 方言差异、schema 对账、驱动错误、能力位、事务重试、NULL 排序与时间 | `memory/dialect.md` |
| 导出面、命名、兼容别名、Runtime 选项、v5 核心重写、根包依赖 | `memory/api.md` |
| 查询构建、分页、可空性、子查询与 CTE、列表参数、关键词搜索 | `memory/query.md` |
| 按条件写、软删除、乐观锁、批量写与 upsert、部分列写回 | `memory/write.md` |
| `//tsq:` 指令、生成的表声明、字段形状、生成物与版本号、CLI | `memory/codegen.md` |
| 门禁的设计、集成测试、CI 与 ruleset、发版流程、两份技能 | `memory/harness.md` |

新记录写进对应的那一份，不要写回这里；这里只放判据和索引。找不到该归哪份就归离它最近的主题，不要新开一份只装一条记录。
